{
  "description": "Defensive setup for the bundled Augenmaß Workbench binary.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "chmod +x \"${CLAUDE_PLUGIN_ROOT}/bin/augenmass\" 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
